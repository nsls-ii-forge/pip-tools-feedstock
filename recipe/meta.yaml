{% set name = "pip-tools" %}
{% set version = "6.3.1" %}
{% set sha256 = "992d968df6f1a19d4d37c53b68b3d4b601b894fb3ee0926d1fa762ebc7c7e9e9" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  noarch: python
  entry_points:
    - pip-compile = piptools.scripts.compile:cli
    - pip-sync = piptools.scripts.sync:cli
  script: {{ PYTHON }} -m pip install . -vv --no-deps

requirements:
  host:
    - python
    - pip
    - setuptools_scm
  run:
    - python
    - click
    - pep517

test:
  requires:
    - pip
  imports:
    - piptools
  commands:
    - pip check
    - pip-compile --help
    - pip-sync --help

about:
  home: https://github.com/jazzband/pip-tools
  license_family: BSD
  license: BSD-3-Clause
  license_file: LICENSE
  summary: pip-tools keeps your pinned dependencies fresh.
